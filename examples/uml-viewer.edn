;; Generated by clj -M:ir from the policy file. Do not edit.
{:title "UML viewer",
 :diagrams
 [{:title "Layers",
   :direction :tb,
   :packages
   [{:id :main,
     :label "Main",
     :classes
     [{:id :main.uml-viewer, :name "MainUmlViewer", :hide-members true}
      {:id :main.ir-generator, :name "MainIrGenerator", :hide-members true}]}
    {:id :adapters,
     :label "Adapters",
     :classes
     [{:id :draw, :name "Draw", :hide-members true}
      {:id :sketch, :name "Sketch", :hide-members true}
      {:id :core, :name "Core", :hide-members true}
      {:id :source-window, :name "SourceWindow", :hide-members true}
      {:id :ir-generator, :name "IrGenerator", :hide-members true}]}
    {:id :app,
     :label "Application",
     :classes
     [{:id :events, :name "Events", :hide-members true}
      {:id :detail, :name "Detail", :hide-members true}]}
    {:id :engine,
     :label "Engine",
     :classes
     [{:id :layout, :name "Layout", :hide-members true}
      {:id :route, :name "Route", :hide-members true}
      {:id :curve, :name "Curve", :hide-members true}
      {:id :compose, :name "Compose", :hide-members true}
      {:id :hit, :name "Hit", :hide-members true}]}
    {:id :source-factory,
     :label "SourceFactory",
     :classes
     [{:id :source.clojure, :name "SourceClojure", :hide-members true}
      {:id :graph.clojure, :name "GraphClojure", :hide-members true}]}
    {:id :language-implementation,
     :label "LanguageImplementation",
     :classes
     [{:id :graph, :name "Graph", :stereotype :interface, :hide-members true}
      {:id :source, :name "Source", :stereotype :interface, :hide-members true}]}
    {:id :domain,
     :label "Domain",
     :classes
     [{:id :ir, :name "Ir", :hide-members true}
      {:id :geom, :name "Geom", :hide-members true}
      {:id :metrics, :name "Metrics", :hide-members true}
      {:id :theme, :name "Theme", :hide-members true}
      {:id :overlay, :name "Overlay", :hide-members true}
      {:id :source-html, :name "SourceHtml", :hide-members true}
      {:id :grok, :name "Grok", :hide-members true}
      {:id :policy, :name "Policy", :hide-members true}]}],
   :edges
   [{:from :core, :to :grok, :kind :dependency}
    {:from :detail, :to :metrics, :kind :dependency}
    {:from :events, :to :hit, :kind :dependency}
    {:from :sketch, :to :source-window, :kind :dependency}
    {:from :events, :to :detail, :kind :dependency}
    {:from :compose, :to :geom, :kind :dependency}
    {:from :main.uml-viewer, :to :core, :kind :dependency}
    {:from :grok, :to :theme, :kind :dependency}
    {:from :events, :to :compose, :kind :dependency}
    {:from :draw, :to :curve, :kind :dependency}
    {:from :draw, :to :geom, :kind :dependency}
    {:from :hit, :to :geom, :kind :dependency}
    {:from :layout, :to :geom, :kind :dependency}
    {:from :compose, :to :route, :kind :dependency}
    {:from :draw, :to :hit, :kind :dependency}
    {:from :main.ir-generator, :to :graph.clojure, :kind :dependency}
    {:from :route, :to :geom, :kind :dependency}
    {:from :events, :to :layout, :kind :dependency}
    {:from :sketch, :to :detail, :kind :dependency}
    {:from :core, :to :events, :kind :dependency}
    {:from :draw, :to :metrics, :kind :dependency}
    {:from :detail, :to :overlay, :kind :dependency}
    {:from :route, :to :metrics, :kind :dependency}
    {:from :sketch, :to :events, :kind :dependency}
    {:from :graph.clojure, :to :graph, :kind :implements}
    {:from :sketch, :to :draw, :kind :dependency}
    {:from :core, :to :sketch, :kind :dependency}
    {:from :detail, :to :hit, :kind :dependency}
    {:from :events, :to :route, :kind :dependency}
    {:from :main.ir-generator, :to :ir-generator, :kind :dependency}
    {:from :ir-generator, :to :policy, :kind :dependency}
    {:from :draw, :to :theme, :kind :dependency}
    {:from :sketch, :to :grok, :kind :dependency}
    {:from :main.uml-viewer, :to :source.clojure, :kind :dependency}
    {:from :source.clojure, :to :source, :kind :implements}
    {:from :events, :to :metrics, :kind :dependency}
    {:from :events, :to :ir, :kind :dependency}
    {:from :layout, :to :metrics, :kind :dependency}
    {:from :source-window, :to :source, :kind :dependency}
    {:from :source-window, :to :source-html, :kind :dependency}
    {:from :compose, :to :metrics, :kind :dependency}
    {:from :events, :to :overlay, :kind :dependency}
    {:from :ir-generator, :to :graph, :kind :dependency}
    {:from :compose, :to :layout, :kind :dependency}
    {:from :curve, :to :geom, :kind :dependency}
    {:from :draw, :to :detail, :kind :dependency}]}
  {:title "Main",
   :direction :lr,
   :packages
   [{:id :main,
     :label "Main",
     :classes
     [{:id :main.uml-viewer, :name "MainUmlViewer"}
      {:id :main.ir-generator, :name "MainIrGenerator"}
      {:id :graph.clojure, :name "GraphClojure", :hide-members true}
      {:id :ir-generator, :name "IrGenerator", :hide-members true}
      {:id :core, :name "Core", :hide-members true}
      {:id :source.clojure, :name "SourceClojure", :hide-members true}]}],
   :edges
   [{:from :main.ir-generator, :to :graph.clojure, :kind :association}
    {:from :main.ir-generator, :to :ir-generator, :kind :association}
    {:from :main.uml-viewer, :to :core, :kind :association}
    {:from :main.uml-viewer, :to :source.clojure, :kind :association}]}
  {:title "Adapters",
   :direction :lr,
   :packages
   [{:id :adapters,
     :label "Adapters",
     :classes
     [{:id :draw, :name "Draw"}
      {:id :sketch, :name "Sketch"}
      {:id :core, :name "Core"}
      {:id :source-window, :name "SourceWindow"}
      {:id :ir-generator, :name "IrGenerator"}
      {:id :events, :name "Events", :hide-members true}
      {:id :grok, :name "Grok", :hide-members true}
      {:id :geom, :name "Geom", :hide-members true}
      {:id :curve, :name "Curve", :hide-members true}
      {:id :detail, :name "Detail", :hide-members true}
      {:id :hit, :name "Hit", :hide-members true}
      {:id :metrics, :name "Metrics", :hide-members true}
      {:id :theme, :name "Theme", :hide-members true}
      {:id :graph, :name "Graph", :stereotype :interface, :hide-members true}
      {:id :policy, :name "Policy", :hide-members true}
      {:id :source, :name "Source", :stereotype :interface, :hide-members true}
      {:id :source-html, :name "SourceHtml", :hide-members true}]}],
   :edges
   [{:from :core, :to :grok, :kind :dependency}
    {:from :sketch, :to :source-window, :kind :dependency}
    {:from :draw, :to :curve, :kind :dependency}
    {:from :draw, :to :geom, :kind :dependency}
    {:from :draw, :to :hit, :kind :dependency}
    {:from :sketch, :to :detail, :kind :dependency}
    {:from :core, :to :events, :kind :dependency}
    {:from :draw, :to :metrics, :kind :dependency}
    {:from :sketch, :to :events, :kind :dependency}
    {:from :sketch, :to :draw, :kind :dependency}
    {:from :core, :to :sketch, :kind :association}
    {:from :ir-generator, :to :policy, :kind :dependency}
    {:from :draw, :to :theme, :kind :dependency}
    {:from :sketch, :to :grok, :kind :dependency}
    {:from :source-window, :to :source, :kind :dependency}
    {:from :source-window, :to :source-html, :kind :dependency}
    {:from :ir-generator, :to :graph, :kind :dependency}
    {:from :draw, :to :detail, :kind :dependency}]}
  {:title "Application",
   :direction :lr,
   :packages
   [{:id :app,
     :label "Application",
     :classes
     [{:id :events, :name "Events"}
      {:id :detail, :name "Detail"}
      {:id :hit, :name "Hit", :hide-members true}
      {:id :metrics, :name "Metrics", :hide-members true}
      {:id :overlay, :name "Overlay", :hide-members true}
      {:id :compose, :name "Compose", :hide-members true}
      {:id :ir, :name "Ir", :hide-members true}
      {:id :layout, :name "Layout", :hide-members true}
      {:id :route, :name "Route", :hide-members true}]}],
   :edges
   [{:from :detail, :to :metrics, :kind :dependency}
    {:from :events, :to :hit, :kind :dependency}
    {:from :events, :to :detail, :kind :dependency}
    {:from :events, :to :compose, :kind :association}
    {:from :events, :to :layout, :kind :dependency}
    {:from :detail, :to :overlay, :kind :dependency}
    {:from :detail, :to :hit, :kind :dependency}
    {:from :events, :to :route, :kind :dependency}
    {:from :events, :to :metrics, :kind :dependency}
    {:from :events, :to :ir, :kind :dependency}
    {:from :events, :to :overlay, :kind :dependency}]}
  {:title "Engine",
   :direction :lr,
   :packages
   [{:id :engine,
     :label "Engine",
     :classes
     [{:id :layout, :name "Layout"}
      {:id :route, :name "Route"}
      {:id :curve, :name "Curve"}
      {:id :compose, :name "Compose"}
      {:id :hit, :name "Hit"}
      {:id :geom, :name "Geom", :hide-members true}
      {:id :metrics, :name "Metrics", :hide-members true}]}],
   :edges
   [{:from :compose, :to :geom, :kind :dependency}
    {:from :hit, :to :geom, :kind :dependency}
    {:from :layout, :to :geom, :kind :dependency}
    {:from :compose, :to :route, :kind :association}
    {:from :route, :to :geom, :kind :dependency}
    {:from :route, :to :metrics, :kind :dependency}
    {:from :layout, :to :metrics, :kind :dependency}
    {:from :compose, :to :metrics, :kind :dependency}
    {:from :compose, :to :layout, :kind :association}
    {:from :curve, :to :geom, :kind :dependency}]}
  {:title "SourceFactory",
   :direction :lr,
   :packages
   [{:id :source-factory,
     :label "SourceFactory",
     :classes
     [{:id :source.clojure, :name "SourceClojure"}
      {:id :graph.clojure, :name "GraphClojure"}
      {:id :graph, :name "Graph", :stereotype :interface, :hide-members true}
      {:id :source, :name "Source", :stereotype :interface, :hide-members true}]}],
   :edges
   [{:from :graph.clojure, :to :graph, :kind :implements}
    {:from :source.clojure, :to :source, :kind :implements}]}
  {:title "LanguageImplementation",
   :direction :lr,
   :packages
   [{:id :language-implementation,
     :label "LanguageImplementation",
     :classes
     [{:id :graph, :name "Graph", :stereotype :interface}
      {:id :source, :name "Source", :stereotype :interface}]}],
   :edges []}
  {:title "Domain",
   :direction :lr,
   :packages
   [{:id :domain,
     :label "Domain",
     :classes
     [{:id :ir, :name "Ir"}
      {:id :geom, :name "Geom"}
      {:id :metrics, :name "Metrics"}
      {:id :theme, :name "Theme"}
      {:id :overlay, :name "Overlay"}
      {:id :source-html, :name "SourceHtml"}
      {:id :grok, :name "Grok"}
      {:id :policy, :name "Policy"}]}],
   :edges [{:from :grok, :to :theme, :kind :dependency}]}]}
